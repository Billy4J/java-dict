<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.afterAll.mybatis.mapper.UserMapper">

    <!--int insertUser();-->
    <insert id="addUser">
        insert into t_user
        values (34, 'love you', null)
    </insert>

    <delete id="deleteUser">
        delete
        from t_user
        where id = 33
    </delete>

    <update id="updateUser">
        update t_user
        set name="yes i do"
        where id = 31
    </update>

    <select id="getUserById" resultType="User">
        select *
        from t_user
        where id = 1
    </select>

    <select id="getAllUser" resultType="User">
        select *
        from t_user
    </select>

</mapper>


