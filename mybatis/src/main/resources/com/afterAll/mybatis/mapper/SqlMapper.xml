<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.afterAll.mybatis.mapper.SqlMapper">

    <select id="getUsersByFuzzy" resultType="User">
        select *
        from t_user
        <!--where name like "%"#{fuzzy}"%"-->
        <!--where name like '%${fuzzy}%'-->
        where name like concat('%',#{mohu},'%')
    </select>

    <delete id="deleteMore">
        delete
        from t_user
        <!-- 使用 in 关键字，是字符串拼接的模式，不能使用#{}的占位符方式 -->
        where id in (${ids})
    </delete>

    <select id="getAllUser" resultType="User">
        select *
        from ${tableName}
    </select>

    <!--int insertUser(User user);-->
    <insert id="insertUser" useGeneratedKeys="true" keyProperty="id">
        insert into t_user
        values (null, #{name}, #{birthday})
    </insert>

</mapper>


